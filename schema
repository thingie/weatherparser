CREATE TABLE records
(date text,
windDirection text,
windSpeed int,
airPressure real,
temperature real,
dewPoint real,
relativeHumidity int,
rainfall text,
station text,
originalData text,
unique(date, station));

CREATE TABLE errors
(date text,
checksum text,
badtext text,
unique(date, checksum));
